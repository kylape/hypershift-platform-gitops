apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: poc-cluster-cert-manager
  namespace: openshift-gitops
spec:
  project: hypershift-platform
  source:
    repoURL: https://github.com/kylape/hypershift-platform-gitops.git
    targetRevision: main
    path: platform/apps/cert-manager
  destination:
    name: poc-cluster
    namespace: cert-manager
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
